# Cleanup candidates — flagging pass (REPORT ONLY)

Generated by a flagging scan. **Nothing has been deleted, moved, or edited.**
Every row is a candidate awaiting (a) user approval and (b) a separate
verification swarm fact-check. Bias is conservative: when reachability was
ambiguous, the row is marked low confidence and "keep pending verification."

Scope notes:
- `results/`, `data/`, `node_modules/`, `design/extracted/`, `.env*` were left
  untouched (intentionally gitignored artifacts).
- `docs/handoffs/archive/` and `docs/optimization-attempts/v3-state-pre-cuda-graphs/`
  are **deliberate, documented archives** (the latter is an explicit pre-CUDA-graphs
  diff snapshot per `docs/optimization-attempts/README.md`). They are NOT flagged.
- Reachability was tested by grepping the whole repo (imports, `.sh`, configs,
  `package.json`, `playwright.config.ts`, docs) excluding the file itself.

Legend for Recommendation: delete / combine-with-X / split / gitignore-and-untrack / keep-pending-verification.

---

## 1. Orphaned / unused files

| Path | Category | Reason | Confidence | How to verify | Recommendation |
|---|---|---|---|---|---|
| `src/stage2/data/build_manifest.py` | Orphan (superseded) | Builds the Net 4 manifest from **ASL Citizen**; current Net 4 round uses PopSign via `build_manifest_popsign.py` (per `HANDOFF_WEBGPU_E2E.md` / `net4_data_sourcing.md`). **Zero importers** anywhere (`-m src.stage2.data.build_manifest` not invoked by any script/config/test). | high | `grep -rn "build_manifest\b" --include=*.py --include=*.sh . \| grep -v build_manifest_popsign \| grep -v build_manifest.py` → empty | delete (after confirming ASL Citizen path is abandoned) |
| `scripts/bench_callback.py` | Orphan (concluded bench) | One-off DALI callback timing probe from the optimization round (now closed; see `optimization-attempts/README.md` §4). Zero references; not in any launcher. | med | `grep -rn "bench_callback" . --exclude-dir=node_modules` → self only | delete (or move to `docs/optimization-attempts/`) |
| `scripts/bench_callback_breakdown.py` | Orphan (concluded bench) | Companion breakdown probe to the above; same closed round. Zero references. | med | same grep, stem `bench_callback_breakdown` | delete (or archive) |
| `scripts/plot_dataset_mixes.py` | Orphan (untracked) | New untracked plotting script; zero references; not wired to any pipeline. Likely a one-off chart gen. | med | `git status` shows untracked; `grep -rn plot_dataset_mixes .` → none | keep-pending-verification (confirm not needed for a pending writeup) |
| `scripts/plot_net3_aws_options.py` | Orphan (untracked) | Untracked one-off cost/option plot; zero references. | med | untracked; `grep -rn plot_net3_aws_options .` → none | keep-pending-verification |
| `scripts/plot_net3_gpu_options.py` | Orphan (untracked) | Untracked one-off cost/option plot; zero references. | med | untracked; `grep -rn plot_net3_gpu_options .` → none | keep-pending-verification |
| `scripts/precompute_boxes.py` | Orphan? (manual trigger) | Idempotent box-precompute trigger; zero references. May be a hand-run prep step for Net 2 cache (calls live `DALIDetectorLoader._precompute_boxes`). | low | `grep -rn precompute_boxes . --exclude-dir=node_modules`; ask whether Net 2 retrain still needs it | keep-pending-verification |
| `scripts/ca_v2_orchestrate.sh` | Orphan (old-round orchestrator) | Drives the **v2** trainers (`train_v2`, `train_v2_facebody`). Zero references; current round is v3. | med | `grep -rn ca_v2_orchestrate .` → self only; cross-check it only calls v2 entrypoints | keep-pending-verification (delete only if v2 family is retired — see §2) |
| `scripts/nj_orchestrate.sh` | Orphan (old-round orchestrator) | References `train.py` + `train_v2` + `predict_bboxes_for_phase2` (all v1/v2-era). Zero references. | med | `grep -rn nj_orchestrate .` → self only | keep-pending-verification |
| `scripts/launch_training.sh` | Orphan (old-round launcher) | Calls v1/v2 `train.py`/`train_v2`. Zero references; superseded by `launch_v3.sh`. | med | `grep -rn launch_training.sh .` → self only | keep-pending-verification |
| `scripts/launch_v3_net2_only.sh` | Orphan (subsumed launcher) | Net-2-only launcher; zero references; superseded by `launch_net2_v3_1.sh` for the current round. | low | `grep -rn launch_v3_net2_only .` → self only; confirm not used in a queued Net 2 retrain | keep-pending-verification |
| `scripts/check_dual_status.sh` | Orphan (Modal-era) | Status poller that references `modal_apps/train_net1.py`/`train_net3.py`. Zero references; Modal path appears superseded by vast/AWS/runpod. | low | `grep -rn check_dual_status .`; confirm Modal not in current infra | keep-pending-verification |
| `scripts/run_smoke_matrix.sh` | Orphan? | Zero references; smoke-matrix runner. May be hand-run CI. | low | `grep -rn run_smoke_matrix .` → self only | keep-pending-verification |
| `scripts/screenshot-all-pages.ts` | Orphan? (dev tool) | Not wired into `package.json` scripts or `playwright.config.ts`; zero references. Hand-run screenshot util. | low | `grep -n screenshot package.json playwright.config.ts` → none | keep-pending-verification |
| `scripts/screenshot-superbuilders.ts` | Orphan? (dev tool) | Same as above; not wired anywhere. | low | same | keep-pending-verification |
| `frontend/src/components/ui/badge.tsx` | Orphan (unused UI primitive) | No imports anywhere in `frontend/src`. (The only "badge" hit is a prose comment in `MasteryBar.tsx`, not an import.) | high | `grep -rn "ui/badge\|Badge" frontend/src \| grep -v ui/badge.tsx` → comment only | delete |
| `frontend/src/components/ui/tabs.tsx` | Orphan (unused UI primitive) | No imports and no `Tabs` usage anywhere in `frontend/src`. | high | `grep -rn "ui/tabs\|Tabs" frontend/src \| grep -v ui/tabs.tsx` → empty | delete |

Not flagged (verified live despite looking suspicious):
- `src/stage2/data/extract_keypoints_batched.py` — imported by `extract_keypoints.py` (live helper). Keep.
- `src/stage1/models/anchors_numba.py` + `scripts/verify_numba_anchors.py` — wired via `train_v3_detector.py --use-numba-anchors`. Keep.
- `frontend/src/main.tsx` — entry point referenced by `index.html`. Keep.
- `tools/onnx_export/*` — used by the active WebGPU port. Keep.

---

## 2. Duplicate / overlapping modules

| Path(s) | Category | Reason | Confidence | How to verify | Recommendation |
|---|---|---|---|---|---|
| `src/stage1/train.py`, `train_v2.py`, `train_v2_facebody.py` | Duplicate generation (v1/v2 trainers) | Three superseded trainer generations. The current round trains via `train_v3_detector.py` + `train_v3_landmark_reg.py`. v1/v2 are reachable only from the orphan orchestrators (`nj_/ca_v2_/launch_training.sh`) + archived handoffs + the Modal net1 apps. | med | Confirm no **current** launcher/config invokes them: `grep -rln "train_v2\|stage1.train\b" scripts/ configs/` returns only the orphan orchestrators in §1 | combine/retire as one decision: keep `train_v3_*` only; delete the v1/v2 trio once the user confirms no v2 retrain is queued |
| `src/stage1/augment/transforms.py`, `transforms_v2.py` | Duplicate (superseded augment) | `transforms` used only by v1 (`train.py`, `eval.py`, `cached.py`, `unified.py`); `transforms_v2` used only by the v2 trainers + `cached.py`. Current round uses `transforms_v3`. | med | `grep -rn "augment.transforms_v2\b\|augment.transforms\b" src scripts` — all consumers are v1/v2 | delete with the v1/v2 trainers (same decision as the trainer trio); blocked while `cached.py` still imports them |
| `src/stage1/losses.py` | Duplicate (superseded loss) | Used only by v1/v2 (`train.py`, `train_v2*`, `eval.py`, `smoke_test.py`). Current round uses `losses_v3.py` + `losses_landmark_reg.py`. | med | `grep -rn "stage1.losses\b" src scripts tests` — all v1/v2 | delete with the v1/v2 family decision |
| `src/stage1/train_v3_landmark.py` (heatmap) vs `train_v3_landmark_reg.py` (regression) | Overlapping (old vs new Net 3 head) | Per `HANDOFF_REGRESSION_ROUND.md`, Net 3 was rewritten heatmap→regression; `_reg` is the current trainer. `train_v3_landmark.py` (+ `HandLandmarkNet` heatmap model, `losses_v3` landmark path) is the prior head, still referenced by `launch_v3.sh`, `_remote_train.sh`, `extract_keypoints.py`. | low | Confirm the **deployed** Net 3 ckpt came from `_reg`; check whether `extract_keypoints.load_net3` still needs the heatmap `HandLandmarkNet` path | keep-pending-verification (do NOT delete — heatmap path is still wired into inference/extract; only retire after the pipeline fully drops heatmap Net 3) |
| `modal_apps/train_net1.py` vs `modal_apps/train_net1_v3_1.py` | Duplicate (Modal net1 trainers) | Two Modal net1 launchers (207 vs 155 LOC). `train_net1.py` referenced only by the orphan `check_dual_status.sh`; `_v3_1` referenced by no script (docs only). Modal appears superseded by vast/AWS/runpod infra. | low | Confirm Modal is no longer the training backend; `grep -rn modal_apps scripts/` → only `check_dual_status.sh` + `launch_net2_v3_1.sh` | keep-pending-verification (retire whole `modal_apps/` if Modal is abandoned) |
| `scripts/extract_keypoints` path vs `extract_keypoints_batched` | NOT a duplicate | `extract_keypoints.py` orchestrates and imports `_batched` for the fast path. | high | already verified import edge | keep both |

---

## 3. Stale / superseded docs

| Path | Category | Reason | Confidence | How to verify | Recommendation |
|---|---|---|---|---|---|
| `docs/GOAL_STATE.md` | Stale doc | Referenced only by an **archived** handoff (`archive/HANDOFF_NET3_V2.md`); no current handoff/doctrine points to it. Likely a pre-regression-round goal snapshot. | low | `grep -rn GOAL_STATE.md .` → only archived handoff | keep-pending-verification (consider moving to `docs/handoffs/archive/` or a `docs/archive/`) |
| `docs/v3-plan.md` | Stale doc | Referenced only by `archive/HANDOFF_STAGE1.md` + `optimization-attempts/README.md`. The v3 plan is largely executed; current direction lives in the MediaPipe-gap + WebGPU handoffs. | low | `grep -rn v3-plan.md .` → archive + optimization-attempts only | keep-pending-verification (archive, don't delete — still a useful historical record) |
| `docs/aws_quota_appeal.md` | Stale doc | One-off AWS quota appeal text; referenced only by `archive/HANDOFF_V3_1.md`. Operational artifact, not doctrine. | low | `grep -rn aws_quota_appeal .` → archived handoff only | keep-pending-verification |
| `docs/VALIDATION_REPORT.md` | Possibly current | Referenced by **current** `HANDOFF_WEBGPU_E2E.md` + `HANDOFF_REGRESSION_ROUND.md`. | high (keep) | already verified live refs | keep |

Not flagged (verified current or doctrine): `docs/principles.md`, `docs/ml-handoff.md`,
`docs/ux-spec.md`, `docs/training-plan.md`, `docs/hoyso-architecture.md`,
`docs/WEBGPU_PORT_PLAN.md`, `docs/PRIVACY.md`, `docs/DATASET_AND_TRAINING.md`,
`docs/local-setup.md`, `docs/prd-scaffold.md`, all `docs/handoffs/*` (current),
`docs/handoffs/archive/*` (deliberate archive), `docs/competitive/*`,
`docs/research/*`, `docs/superbuilders/*`, `docs/optimization-attempts/*`
(documented snapshot/log).

---

## 4. Root-level clutter

| Path | Category | Reason | Confidence | How to verify | Recommendation |
|---|---|---|---|---|---|
| `superbuilders-partner-project-asl-learning-with-computer-vision.pdf` | Tracked binary at root | **Tracked** 210 KB PDF (the assignment brief). Binary committed to repo root; reference material, not source. | high | `git ls-files superbuilders-*.pdf` → matches | keep-pending-verification (if it must stay, move to `docs/`; otherwise gitignore-and-untrack). Not urgent — small. |
| `google_iso_1st_place.md` | Tracked stray doc at root | **Tracked** 8 KB reference note (Google ISLR 1st-place writeup) sitting at repo root instead of `docs/research/`. | high | `git ls-files google_iso_1st_place.md` → matches | combine-with-X: move into `docs/research/` (relocate, not delete) |
| `.scaffold-final-report.md` | Tracked scaffold artifact at root | **Tracked**, but its siblings `.scaffold-phase*-report.md` are gitignored (the glob `*-phase*-report` misses "final"). Inconsistent: a scaffold-time report that should match the others' gitignore treatment. | high | `git ls-files \| grep scaffold` → only this one + `docs/prd-scaffold.md`; `.gitignore` shows `.scaffold-phase*-report.md` | gitignore-and-untrack (add `.scaffold-final-report.md` or broaden to `.scaffold-*-report.md`) and `git rm --cached` |
| `ASL_Citizen.zip` (45 GB) | Large binary at root | Already **gitignored and untracked** (good). Flagged only for awareness: 45 GB dataset zip living at repo root. | high | `git ls-files ASL_Citizen.zip` → empty; `.gitignore` lists it | keep (not in git); optional: relocate under `data/` for tidiness |
| `ruvector.db` (1.5 MB) | Local DB at root | Gitignored + untracked (ruflo vector store). Awareness only. | high | `git ls-files ruvector.db` → empty; `.gitignore` lists it | keep (not in git) |
| `.round_env` | Ephemeral state at root | Gitignored + untracked (training-round host/port/secrets). Awareness only — do not commit. | high | `.gitignore` lists `.round_env` | keep (not in git) |
| `screenshots/`, `test-results/`, `logs/`, `playground/`, `costs/` | Local-only dirs | All gitignored, **zero tracked files**. No action. | high | `git ls-files <dir>` → empty for each | keep (not in git) |

---

## 5. Oversized files (>500-line project limit) — SPLIT, not delete

| Path | Lines | Category | Suggested split boundary | Confidence | Recommendation |
|---|---|---|---|---|---|
| `src/stage1/data/dali_pipelines.py` | 910 | Oversized | Two classes already separate cleanly: keep `DALIDetectorLoader` (lines ~87–720) in `dali_detector.py` and move `DALILandmarkLoader` (~722–910) to `dali_landmark.py`; lift the module-level helpers (`_normalize_bbox_to_input`, `_build_landmark_affine`, `_read_jpeg_with_dims`) into a shared `dali_common.py`. | high | split |
| `src/stage2/data/extract_keypoints.py` | 671 | Oversized | Split by net: `load_net1/run_net1/_decode_box_kpts` → `net1_runner.py`; `load_net2/run_net2/_expand_box` → `net2_runner.py`; `load_net3/run_net3/_net3_one_pass` → `net3_runner.py`; keep `iter_video_frames`, `assign_hand_side`, `extract_one_clip`, `process_one`, `main` as the orchestrator in `extract_keypoints.py`. (`extract_keypoints_batched.py` already factors the batched path.) | high | split |
| `src/stage1/models/anchors.py` | 592 | Oversized | Core anchor gen + encode/decode + match + nms (lines ~41–353) stay in `anchors.py`; move the two abandoned GPU matchers `build_targets_gpu` (~355) and `build_targets_gpu_batched` (~448) — both documented as "kept in tree, off" — into `anchors_gpu_experimental.py`. Saves ~240 lines and isolates dead-but-kept code. | high | split |
| `src/stage1/train_v3_detector.py` | 584 | Oversized | Extract the arg-parser + config-resolution block (top `add_argument`s) into `train_v3_detector_args.py`, and the anchor-target build branch (numba/gpu/numpy dispatch around lines ~329–475) into a helper module, leaving the train loop in the main file. | med | split |
| `src/stage1/train_v3_landmark.py` | 535 | Oversized | Mirror the `_reg` sibling's pattern: it already has `train_v3_landmark_helpers.py`; move more of the dataset/loader setup and the eval/PCK block into that helper to get under 500. (Lower priority — heatmap Net 3 may be retired; see §2.) | low | split (or moot if heatmap Net 3 is retired) |
| `frontend/src/pages/Practice.tsx` | 502 | Oversized (barely) | Pull the pure builder helpers `buildSignsFromBackend`, `toFullSignDrills`, `applyLessonConfig`, `computeContinueLabel` (~lines 48–110, 466–479) into `practice-helpers.ts`, and `CameraPanelWrapper` (~480+) into its own component file. Drops the page comfortably under 500. | high | split |

---

## Summary counts

- **Orphaned/unused:** 16 candidates (2 high-confidence deletes: `build_manifest.py`, frontend `badge.tsx`/`tabs.tsx` — 3 files; remainder med/low).
- **Duplicate/overlapping:** 6 groups (v1/v2 trainer+transforms+losses family is the big retire-as-one decision; Modal net1 pair; heatmap vs regression Net 3 kept pending).
- **Stale docs:** 3 low-confidence (archive, don't delete); rest verified current.
- **Root clutter:** 3 tracked items to address (`.scaffold-final-report.md` untrack, `google_iso_1st_place.md` relocate, brief PDF relocate-or-untrack); large binaries already gitignored.
- **Oversized:** 6 files to split (4 high-confidence split boundaries identified).
