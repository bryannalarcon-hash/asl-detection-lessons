/**
 * Renders the Account Settings route. It is a placeholder screen backed by
 * PlaceholderLayout, standing in for the not-yet-built content described in UX spec section 15.
 */
import { PlaceholderLayout } from '@/components/layout/PlaceholderLayout';

export default function AccountSettingsPage() {
  return (
    <PlaceholderLayout title="Account settings" uxSpecSection="§15 Account Settings" />
  );
}
