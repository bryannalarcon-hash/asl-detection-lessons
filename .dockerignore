# Reinstalled / rebuilt inside the image
node_modules
**/node_modules
**/dist
.git

# Huge non-app artifacts (model checkpoints, datasets, venvs)
/results
/data
/.venv-demo
/.venv
*.zip
*.pdf
*.tgz
ruvector.db

# Python CV training code + infra (not part of the web app)
/src
/modal_apps
/tools
/playground
scripts/*.py
scripts/*.sh

# Docs / dev artifacts not needed at runtime
/docs
/design
/costs
/logs
/screenshots
/test-results
/tests
.scaffold-*-report.md
*.log

# NOTE: frontend/public (lesson videos + ONNX models) is intentionally KEPT —
# the app serves those. configs/, scripts/*.ts, backend/, frontend/ are kept.
